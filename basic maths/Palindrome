class Solution {
public:
    bool isPalindrome(int n) {
int copy = n;
int revnum = 0;
while(n>0){
    int lastDigit = n % 10;
    revnum = revnum * 10+ lastDigit;
    n = n/10;
}
if(revnum == copy) return true;
return false;
    }
};
